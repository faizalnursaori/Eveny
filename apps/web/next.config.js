/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "img.daisyui.com",
      "example.com",
      "localhost",
    ],
  },
};

module.exports = nextConfig;
